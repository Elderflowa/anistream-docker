services:
  app:
    image: anilist:latest
    container_name: anilist
    ports:
      - "6969:7000"   # host:container
    environment:
      - PORT=7000
      - HOST=0.0.0.0
    env_file:
      - .env          # optional (only if you use one)
    restart: unless-stopped
